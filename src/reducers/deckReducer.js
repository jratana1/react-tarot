const initialDeck = {
    cards: []
}

const deckReducer = (state= initialDeck, action) => {
    switch(action.type){
        default:
            return {...state}
    }
}

export default deckReducer